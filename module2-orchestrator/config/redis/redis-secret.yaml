apiVersion: v1
kind: Secret
metadata:
  name: redis-password
  namespace: module2-system
  labels:
    app: redis
    component: security
    managed-by: cloudtask-operator
    version: "7.2"
  annotations:
    description: "Redis authentication password"
    created-date: "2026-04-19"
    security-level: "confidential"

type: Opaque
data:
  # Password: CloudTaskRedis2024!
  # Base64 encoded value (base64 -e "CloudTaskRedis2024!" = Q2xvdWRUYXNrUmVkaXMyMDI0IQ==)
  password: Q2xvdWRUYXNrUmVkaXMyMDI0IQ==

---
# Secret for Redis TLS/SSL (optional, for future use)
apiVersion: v1
kind: Secret
metadata:
  name: redis-tls
  namespace: module2-system
  labels:
    app: redis
    component: security
    managed-by: cloudtask-operator
  annotations:
    description: "TLS certificates for Redis (optional, unused for now)"
    status: "placeholder-for-future-use"

type: Opaque
data:
  # Placeholder for TLS certificates
  # These would be populated if TLS is enabled in future
